export class BusinessLogic {
    doSomething(a: string) {
        return `Business Logic working on ${a}`;
    }

    doSomethingElse(b: string) {
        return `Business Logic also working on ${b}`;
    }
}